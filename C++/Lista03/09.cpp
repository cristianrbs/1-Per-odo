#include<iostream>
using namespace std;

int main(){

    //Se i e j são variáveis inteiras e p e q ponteiros para int, quais das seguintes expressões de atribuição são incorretas?

/*  a) p == &i
    b) *q == &j //Incorreta
    c) p == &*&i
    d) i = (*&j //Incorreta
    e) i = *&*&j
    f) q = &p //Incorreta
    g) i = (*p)++ + *q
    h) if (p==i) i++ */ //Incorreta

    return 0;
}